#include <gtest/gtest.h>

#include "<project-name>/core/foo.hpp"

// Tests for the `core` module.
TEST(Core, Greeter) {
    <namespace-macro>_NS::core::Greeter greeter("world");
    EXPECT_EQ(greeter.greet(), "Hello, world!");
}
