# src/: aggregates all modules. Each module is self-contained under
# src/<module>/ with its own CMakeLists.txt. Add a module by adding
# add_subdirectory(<module>) here.
add_subdirectory(core)
add_subdirectory(algorithm)
