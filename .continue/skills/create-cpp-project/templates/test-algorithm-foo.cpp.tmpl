#include <gtest/gtest.h>

#include "<PROJECT_NAME_UPPER>_NAMESPACE@/algorithm/foo.hpp"

// Tests for the `algorithm` module.
TEST(Algorithm, Add) {
    EXPECT_EQ(<PROJECT_NAME_UPPER>_NAMESPACE_MACRO@::algorithm::add(1, 2), 3);
}

TEST(Algorithm, GreetSum) {
    // algorithm depends on core, so core types are usable here.
    EXPECT_EQ(<PROJECT_NAME_UPPER>_NAMESPACE_MACRO@::algorithm::greet_sum("world", 1, 2),
              "Hello, world! Sum is 3.");
}
