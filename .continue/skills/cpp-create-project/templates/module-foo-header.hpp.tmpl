#pragma once

#include <string>

#include "<project-name>/config.h"

namespace <namespace-macro>_NS::core {

/// Sample class for the `core` module.
class <PROJECT_NAME_UPPER>_API Greeter {
public:
    explicit Greeter(std::string name);
    std::string greet() const;

private:
    std::string name_;
};

}  // namespace <namespace-macro>_NS::core
