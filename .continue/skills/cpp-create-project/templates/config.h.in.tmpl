// Build-time configuration, generated by CMake from cmake/config.h.in.
// Do not edit directly — edit cmake/config.h.in instead.
#pragma once

// NOTE: every token inside @...@ below is a CMake configure_file variable
// with a FIXED name (PROJECT_*) that contains no template placeholders.
// Placeholder replacement (<project-name>, <PROJECT_NAME_UPPER>, ...)
// can therefore never touch these @...@ tokens.
#define <PROJECT_NAME_UPPER>_VERSION_MAJOR @PROJECT_VERSION_MAJOR@
#define <PROJECT_NAME_UPPER>_VERSION_MINOR @PROJECT_VERSION_MINOR@
#define <PROJECT_NAME_UPPER>_VERSION_PATCH @PROJECT_VERSION_PATCH@
#define <PROJECT_NAME_UPPER>_VERSION "@PROJECT_VERSION@"

// Unified C++ namespace for all modules (default set in cmake/options.cmake;
// override via CMakePresets.json "base" preset or -DPROJECT_NAMESPACE=...).
// Use as namespace name: namespace <PROJECT_NAME_UPPER>_NS { ... }
// or in string contexts: <PROJECT_NAME_UPPER>_NS_NAME
#define <PROJECT_NAME_UPPER>_NS @PROJECT_NAMESPACE@
#define <PROJECT_NAME_UPPER>_NS_NAME "@PROJECT_NAMESPACE@"

// Build options
#cmakedefine01 <PROJECT_NAME_UPPER>_BUILD_TESTS
#cmakedefine01 <PROJECT_NAME_UPPER>_WARNINGS_AS_ERRORS

// Build type: <PROJECT_NAME_UPPER>_DEBUG is 1 in Debug builds, 0 otherwise.
#define <PROJECT_NAME_UPPER>_BUILD_TYPE "@CMAKE_BUILD_TYPE@"
#define <PROJECT_NAME_UPPER>_DEBUG @PROJECT_DEBUG@

// Build metadata (set in cmake/common.cmake)
#define <PROJECT_NAME_UPPER>_GIT_COMMIT "@PROJECT_GIT_COMMIT@"
#define <PROJECT_NAME_UPPER>_BUILD_TIME "@PROJECT_BUILD_TIME@"

// ---------------------------------------------------------------------------
// Unified library export macro (shared by ALL modules).
// Each module defines <PROJECT_NAME_UPPER>_EXPORTS (PRIVATE) while building
// itself; <PROJECT_NAME_UPPER>_STATIC_DEFINE (set when BUILD_SHARED_LIBS=OFF)
// disables dllexport/dllimport entirely for static builds/consumption.
// ---------------------------------------------------------------------------
#if defined(_WIN32) || defined(__CYGWIN__)
#  ifdef <PROJECT_NAME_UPPER>_STATIC_DEFINE
#    define <PROJECT_NAME_UPPER>_API
#  elif defined(<PROJECT_NAME_UPPER>_EXPORTS)
     /* We are building one of this project's libraries */
#    define <PROJECT_NAME_UPPER>_API __declspec(dllexport)
#  else
     /* We are consuming this project's libraries */
#    define <PROJECT_NAME_UPPER>_API __declspec(dllimport)
#  endif
#else
#  if __GNUC__ >= 4
#    define <PROJECT_NAME_UPPER>_API __attribute__((visibility("default")))
#  else
#    define <PROJECT_NAME_UPPER>_API
#  endif
#endif
